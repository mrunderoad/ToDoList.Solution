@{
  Layout = "_Layout";
}

@using ToDoList.Models;
@model List<ToDoList.Models.Item>;

<h1>Completed Items</h1>

@if (@Model.Count == 0)
{
  <h3>No items have been completed yet!</h3>
}

@foreach (Item item in Model)
{
  <li>@Html.DisplayFor(model => item.Completed)</li>
  
}